# Screenshots & field imagery

Drop real product captures and photos here, then reference them from the
"See it in the field" gallery in `index.html` (search for `REAL IMAGES GO HERE`).

Suggested set:
- `map.png`     — the live tactical map
- `comms.png`   — the comms / push-to-talk panel
- `vehicle.png` — a drone / UGV product photo or a field team using ESTAK

Recommended: 16:10 ratio, ≥1200px wide, `.webp` or `.png`.
